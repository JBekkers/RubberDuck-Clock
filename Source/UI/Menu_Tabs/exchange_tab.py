import tkinter as tk


def build_exchange_tab(parent, settings, config):

    tk.Label(
        parent,
        text="Exchange",
        font=("Segoe UI", 14, "bold")
    ).pack(pady=10)

    tk.Label(
        parent,
        text="Comming soon"

    ).pack(
        pady=20
    )