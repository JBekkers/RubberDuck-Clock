import tkinter as tk


def build_cosmetics_tab(parent, settings, config):

    tk.Label(
        parent,
        text="Cosmetics",
        font=("Segoe UI", 14, "bold")
    ).pack(pady=10)

    tk.Label(
        parent,
        text="comming soon"

    ).pack(
        pady=20
    )